# Derived artifacts of the C (libevent/libuv) differential tests — not source.
# build/ : compiled test_* binaries, regenerated by run.sh
# deps/  : vendored libevent/libuv (git clone + cmake/make), regenerated by build_deps.sh
libevent-tests/build/
libevent-tests/deps/
libuv-tests/build/
libuv-tests/deps/
# Per-run heatmap outputs at the suite root (run.sh HEATMAP=1); the reference
# copies live in ref-result*/.
/events.tsv
/stress_heatmap.pdf
