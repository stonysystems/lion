[package]
name = "lion-macro"
version = "0.1.0"
edition = "2021"
authors = ["Lion Team"]
description = "Procedural macros for Lion-V2 async runtime"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full", "parsing"] }
